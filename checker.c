#include "push_swap.h"

#include <stdio.h>

ft_main()
{
	// check valid format input
	// make instructions
	// check a ok (ordre croissant) et b vide

}


int     main(int argc, char **argv)
{
    if (argc >= 2)
		ft_main(argv);
	else
		ft_error("Error\n");
    return (0);
}
